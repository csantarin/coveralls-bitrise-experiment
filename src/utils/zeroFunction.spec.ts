import zeroFunction, { returnZero } from './zeroFunction';

describe('zeroFunction', () => {
	test('should exist', () => {
		expect(zeroFunction).toBeDefined();
	});
});

describe('returnZero', () => {
	test('should exist', () => {
		expect(returnZero).toBeDefined();
	});
});
